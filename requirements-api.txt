# Lean runtime for the FastAPI backend only (no plotting/visualization stack).
# Used by the Dockerfile and the Render backend service.
numpy>=2.3.2
scipy>=1.16.1
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
