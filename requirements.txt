# Minimal runtime for the serverless backend (Vercel auto-detects Python from
# this root requirements.txt). Intentionally lean — numpy + fastapi only.
# SciPy is imported lazily, so rk4/euler/verlet work without it.
#
# For the FULL project (demo scripts, matplotlib/plotly visualization, dev
# tooling) install from pyproject instead:  pip install -e .   (or .[dev])
numpy>=1.26.0
fastapi>=0.115.0
